apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: violating-role-workloads-write
  namespace: policy-test
rules:
- apiGroups: ["apps"]
  resources: ["deployments"]
  verbs: ["create"]
