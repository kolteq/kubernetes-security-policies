apiVersion: security.istio.io/v1
kind: PeerAuthentication
metadata:
  name: app-port-exception
  namespace: default
spec:
  selector:
    matchLabels:
      app: api-gateway
  mtls:
    mode: STRICT
  portLevelMtls:
    8080:
      mode: DISABLE
